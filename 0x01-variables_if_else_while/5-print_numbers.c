#include <stdio.h>

/**
 * main - Entry pint
 *
 * Description: print all single digit number
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int digit = 0;

	while (digi <= 9)
	{
		printf("%i", digit);
		digit++;
	}
	printf("\n");

	return (0);
}
